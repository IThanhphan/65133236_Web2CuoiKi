server:
  port: 8080

spring:
  application:
    name: cuoiki
  datasource:
    url: "jdbc:mysql://localhost:3306/boarding_house_management"
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
